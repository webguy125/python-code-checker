[project]
name = "python-code-checker-api"
version = "0.1.0"
description = "Cloudflare Python Worker backend for Python code auditing and cleanup."
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk",
]
